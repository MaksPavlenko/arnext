import React from 'react';
// import PropTypes from 'prop-types';

export const MediaMinWidh = ({ children }) => {
  return <div className="media-min__width">{children}</div>;
};

export const MediaMaxWidth = ({ children }) => {
  return <div className="media-max__width">{children}</div>;
};

// MadiaQueryWrapper.propTypes = {};

// export default MadiaQueryWrapper;
